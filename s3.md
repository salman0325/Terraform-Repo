```terraform.tf
# <block>		<parameters>{
# 		args
# }
# 	------------------>
# 	resource--------resource-block
# 	local_file------resource-type
# 	my_file--------resource-name
# locals_fiel and my_file are perameter
# 	filename and content---are arguments
# 	------------------>


resource local_file my_file {
	filename = "automate.txt"
	content = "this is my first terrafom file"
}

# 1) terrafom init---terrafom initailize
# 2) ls -a say .terrafom show oga
# 3) terrafom validate ---file check for errro
# 4) terraform plan---output plan
# 5) terraform apply----apply the file
# 6) terraform destroy
# 7) terrafrom apply --auto-approve
```
```terrafrom.tf
provider "aws" {
    region = "ap-south-1"
  
}
#this file configure your aws provider 
# abe jo be anayega the defind regin may oga
```
```terraform.tf
terraform {
  required_providers {
    aws = {
      source = "hashicorp/aws"
      version = "6.22.1"
    }
  }
}
# is file say of aws provider install kar saktay ho 
# 1) then run (terraform init)

# 2) aws cli ko install karan be oga apnay aws account configure karna hoga 
# first install aws cli 
# then creaet a user in aws iam and give permistion s3_full_access and also create access_kay and then run aws configure and put your access_kay and personal_access_key

# 3) then check run (aws s3 ls)
```
```terrform.tf
# this file create a s3 bucket
resource "aws_s3_bucket" "my_bucket" {
    bucket = "terraform-bucket-for-test1"
  
}
```
